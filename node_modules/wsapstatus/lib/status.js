var events = require("events");
var util = require("util");

function wsapStatus()
{
	this.changeStatus = function(varResult){
		process.nextTick(function() {
			this.emit('changeState', varResult);
		}.bind(this));
	};
}

util.inherits(wsapStatus, events.EventEmitter);

module.exports = wsapStatus;